{
  "$schema": "https://json-structure.org/meta/core/v0/#",
  "$id": "https://raw.githubusercontent.com/api-evangelist/dynatrace/refs/heads/main/json-structure/account-management-api-user-create-request-structure.json",
  "description": "Request body for creating or updating a user.",
  "type": "object",
  "properties": {
    "email": {
      "type": "string",
      "format": "email",
      "description": "The email address of the user. Used as the login identifier.",
      "example": "admin@example.com"
    },
    "firstName": {
      "type": "string",
      "description": "The first name of the user.",
      "example": "Production Service"
    },
    "lastName": {
      "type": "string",
      "description": "The last name of the user.",
      "example": "Production Service"
    },
    "groups": {
      "type": "array",
      "description": "The list of group IDs to assign the user to. The user gains all permissions associated with these groups.",
      "items": {
        "type": "string"
      },
      "example": [
        "example-value"
      ]
    }
  },
  "required": [
    "email",
    "firstName",
    "lastName"
  ],
  "name": "UserCreateRequest"
}